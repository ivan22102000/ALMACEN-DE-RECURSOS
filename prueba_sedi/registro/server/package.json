
{
  "name": "api-registro",
  "type": "module",
  "scripts": {
    "dev": "cross-env NODE_ENV=development tsx -r dotenv/config src/index.ts",
    "start": "cross-env NODE_ENV=production tsx -r dotenv/config src/index.ts"
  },
  "dependencies": {
    "@supabase/supabase-js": "^2.0.0",
    "cors": "^2.8.5",
    "dotenv": "^17.0.0",
    "express": "^4.18.0"
  },
  "devDependencies": {
    "cross-env": "^7.0.0",
    "tsx": "^3.0.0",
    "typescript": "^4.0.0"
  }
}
